# importador

Fuente de verdad: vault `AppLotes/docs/Componentes/importador-excel.md`.

Excel v2 (hoja `Datos`): `tipo, codigo, nombre, slug, zona, descripcion, codigo_barrio, numero_lote, metros_cuadrados, precio, moneda, estado, orientacion`.

Unidades importadas = siempre `lote_vacio`. Plantilla descargable desde `/importador/nueva`.
